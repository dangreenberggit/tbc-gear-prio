# Vendored skill content — not our source, formatted by upstream.
.agents/
.claude/

# Local issue tracker + probe scratch (large raw exports, one-off links).
.scratch/

# Captured/generated data must stay byte-identical to what was fetched or
# recorded — reformatting would break the sha256 checks in data/wowsims.lock.json
# and make the raw WCL fixture no longer "raw". See docs/verification-log.md.
data/
test/fixtures/
docs/phase0-probe-summaries/

pnpm-lock.yaml

# buf generate output (PLAN.md §8.1) — reformatting would fight the codegen's
# own output on every regen, and idempotency (the CI check) compares generator
# output to generator output, not to prettier's opinion of it.
packages/core/src/proto/

# Hand-tuned planning prose predating this harness — reformatting would
# rewrite hundreds of lines of tables/code-fences with no behavioral upside.
# New docs written after this commit are expected to pass format:check.
PLAN.md
PLAN-REVIEW.md
docs/stage0-findings.md
docs/verification-log.md
