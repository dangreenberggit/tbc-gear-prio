# Carry-forward

Tickets deferred from a feature-branch review so they survive the merge.
Convention: [`docs/agents/issue-tracker.md`](../../docs/agents/issue-tracker.md).
List: `pnpm issues:open`. Gate: `pnpm merge-ready`.
Handing the open ones to workers: [`DELEGATION.md`](DELEGATION.md) — waves,
file contention, worker prompt. This file stays the chronological log.

## Decisions-so-far

- 2026-07-26 — Created from `docs/reviews/phase-0-close-gates.md`. Cheap fixes
  landed on `phase-0/close-gates`; the rest `Blocks: phase-1`.
- 2026-07-26 — Landing is `pnpm land` only. Open `Blocks: phase-N` tickets
  require `--ack-open-blockers` (or close/re-block), not path-in-review theater.
- 2026-07-28 — From `docs/reviews/phase-1-five-seed-spread.md`: filed 06
  (candidate ungemmed swaps), 07 (se vs stdev), 08 (sources[0] raid view),
  09 (CLI maxPhase from lock), 10 (stage-order test), 11 (retire EP generate
  path / BiS / crafts).
- 2026-07-28 — Resolved 06–09 after stash salvage gem fill + follow-up
  fixes (independent SE, multi-source raid filter, lock-file maxPhase).
- 2026-07-28 — Resolved 10–11 (stage-order test; EP path demoted). Split
  bisTags → 12, raid-recipe crafts → 13. Land blocker scan limited to
  `.scratch/carry-forward/issues/` (no nested worktree copies).
- 2026-07-28 — Resolved 05: CI run 30409397254 showed Linux vs Windows
  empty-JSDoc trailing-space drift; normalize after `buf generate`.
- 2026-07-28 — Filed 14 (carry missing enchant on candidate swap) from
  user review of post gem-preserve P3 rank.
- 2026-07-28 — Candidate swaps migrate worn gems (UI-style) then EP-fill
  only empty sockets. Filed 15 (enchantAppliesToItem parity).
- 2026-07-28 — Resolved 16: race defaults from raid-sim skeleton (ret P2
  Blood Elf), not hardcoded Human. Dig: sim-settings-gap-dig.md.
- 2026-07-28 — Planner review of raid-scoped impl: deleted legacy EP pool
  files/scripts (defects 1–2; finishes 11). Filed 17 (phase-2+ no-source
  gap) and 18 (universe recall / junk-filter measurement).
- 2026-07-29 — Pre-merge review of phase-1/five-seed-spread (4 axes, Opus, fresh
  context). Fixed in review: silent candidate-sim swallow now disclosed as
  Substitution rows; Google Fonts removed from the "self-contained" HTML report;
  two durable-claim comments given re-runnable commands. Filed 19 (second
  adapter for GearSource/Store — PLAN §5 two-adapter rule unmet), 20 (meta gems
  ranked by EP but their stats are effect-blind), 21 (world-boss zone strings not
  canonicalised, wrong boss on 30730), 22 (rank.test.ts duplicates production and
  skips fillOptsForSwap), 23 (spec drift: fullPool, Deps shape, per-tier
  universes, setBonusNote), 24 (standards smells). Appended a blocker to 18
  (CASTER_ONLY_STATS includes SpellDamage, which ret EP prices at 0.17).
  PLAN §14 box 8 amended from "at 1 and at 2" to "two maxPhase values".
- 28 — p5 BiS items outside raid zones (Shard of Contempt et al); mechanism half-built, nothing emits a heroic source
- 29 — a `meta-unsolvable` throw strands its job row `running`; CLOSED 2026-08-04, one catch around the whole post-create body
- 30 — the §14 ViewOptions gate box; re-filed 2026-08-04 `Blocks: phase-2` (it is a Phase 2 box, and `applyView` is unimplemented — not a missing test)
- 31 — `SqliteStore` job ids from `SELECT COUNT(*)` race two writers and reuse ids after a delete; `kv` omits §11's `created_at`. Filed 2026-08-05 from the `phase-2/caches` review, `Blocks: phase-4` — ticket 01 scoped the adapter without deployment, and `MemoryStore` is still the only adapter with a production call site
- 32 — nothing reads `rateLimitData`, so §14 Phase 4's "point budget survives expected concurrency" box has no instrument behind it. Filed 2026-08-05 from the `phase-2/caches` review; blocked in practice on the WCL adapter existing (§5.1)
- 33 — the hit-cap banner is blind to talent hit: the pinned ret preset takes 3/3 Precision (~47 rating), so the fixture character reads 72 of ~142 from gear but sits near 119, understating the shortfall ~2.5×. Filed 2026-08-05 from the `phase-2/disclosure-and-caps` review. `talentsString` is already in the composed request, so the talent half needs no new seam
- 34 — `pnpm verify` never typechecks the test suite (`packages/core/tsconfig.json` includes only `src/**`), which is how an invalid `Race` literal shipped green and asserted itself tautologically. Filed 2026-08-05 from the `phase-2/disclosure-and-caps` review; 18 pre-existing errors across 3 test files
- 35 — `groupBy: 'raid'` buckets a multi-zone item by whichever zone-bearing source comes first in the universe JSON, so the bucket depends on array order. Filed 2026-08-05 from the `phase-2/apply-view` review. Measured: 0 multi-zone entries in `ret-p2.json`, 5 in `ret-p3/p4/p5.json`, so it is unreachable at the current default tier; the fix needs a Phase 3 display decision (appear twice, or one stated rule)
- 36 — §12 says the cutoff applies "within the filtered view", but `CUTOFF` is absolute so the recomputation in `applyView` can never change a value. Filed 2026-08-05 from the `phase-2/apply-view` review (adversarial A3). Either §12 is amended to say absolute, or a relative cutoff is specified — reading 2 collides with §2's "no view changes a number", so the shipped behaviour is the conservative choice, not an oversight; CLOSED 2026-08-06 as absolute (ADR-0020): §12 amended, `applyView`'s redundant pass collapsed to carrying `belowCutoff`, agreement now tested
- 37 — the tier-piece hardening test asserted token `zone` but never `boss`/`token`, and 45 hand-written `ItemSource` literals across the test suite are cross-checked against nothing. Filed 2026-08-05 from the `phase-2/apply-view` review, after a fixture paired the gloves token with Prince Malchezaar (he drops the *helm* token; gloves are The Curator). The curated data was never at fault — `data/two-hop/ret-tokens.json` is well sourced and the universes agree with it on zone, boss and token name. Guard half fixed on that branch; fixture hygiene open
- 38 — `slamaltman-offline.ts` and `report-events-offline.ts` build the same recordings and differ only in `route`, `confidence` and where talents come from. Filed 2026-08-06 from the `phase-2/resolution-and-fallback` review (standards, Duplicated Code). Not hypothetical: the two already disagreed once, the report-events builder hardcoding a ret talent split onto a protection capture, caught by the domain axis rather than a test
- 39 — `belowCutoff` (rank.ts, now also rewritten by paired replication) and `belowCutoffInView` (view.ts) derive the same fact by two paths, and nothing asserts they agree. Filed 2026-08-06 from the `phase-2/resolution-and-fallback` review. Narrowed 2026-08-06 by ticket 36 closing absolute (ADR-0020): the view now carries `belowCutoff` instead of re-deriving it, so there is one path and a test asserts agreement across filters. What remains is 39's paired-replication case
- 40 — fight resolution is not spec-aware: nothing calls `classifySpec` on the resolved fight, so a character who tanks or off-specs some nights can be simmed against the wrong preset and EP weights. Filed 2026-08-06 from `phase-2/resolution-and-fallback` (domain D1 fallout) plus user direction. Real, not hypothetical — ticket 04's first capture was slamaltman's protection night scored as ret. Needs a product decision on the no-matching-fight case; "assume their last fight is their spec" only works once the tool can sim that other spec
- 41 — the ranged slot offered two items and the worn one was not among them. Rewritten 2026-08-06 on `phase-2/feral` around the measurement it had only hypothesised, deleting two wrong causal claims. Cause was not "no db source records" (29 shipping ret rows have none either) but that `wowsims_curated_item_ids()` was read only to *label* rows already admitted, never to grant membership. Largely fixed on that branch — ranged slot 2→4 idols, worn-items-absent 24→14 across three characters — remainder open
- 42 — `map_db_source` emits `crafted.profession` as a raw proto enum number, so the shipped source reads `{"kind":"crafted","profession":"2"}` and reaches the UI as a literal `2`. Filed 2026-08-06 while collecting the feral Wowhead lists. Passes every gate because the union types the field as `string` and the build check only validates `kind`. Wowhead-sourced crafted rows are unaffected, so the same item can carry a good row and a numeric one depending on input
- 43 — random-suffix items ("of the Tiger") cannot be simmed: the pinned db stores only the base item, whose stat map is armour-only, so the three the feral P2 guide recommends will always score near-bottom. Filed 2026-08-06 while collecting the feral Wowhead lists. Per user steer, prefer surfacing "this slot cannot be simmed accurately" over synthesising stats; look at how wowsims models these first. `db.json` does ship a `randomSuffixes` array, so the data exists — untested whether upstream's approach transfers
- 44 — `poolEntryFromUniverse` takes `sources[0]`, which is whichever pipeline appended first (db, atlasloot, two-hop, sunmote, wowhead, curated), not the most actionable origin. Filed 2026-08-06 from the `phase-2/feral` review (adversarial A5). Pre-existing, but the new slashed-zone split makes an arbitrary pick between two equally true raids for multi-zone trash. Same root cause as 35, seen from the pool layer
- 45 — 87 of 627 collected Wowhead rows still parse to no source (zone-less quests, world drops, free-text zone drops), and `zoneKeyOf` renders a literal `unknown` bucket under `--group-by raid`. Filed 2026-08-06 from the `phase-2/feral` review. Much narrower than it was — that branch took feral p1-p2 from 18 unparsed to 7 and p3 from 20 to 2 — and no longer produces a *wrong* source, only an absent one, now that a test forbids a listed item shipping as `unknown`. A `{kind:"world"}` variant already exists and nothing emits it
- 60 — CLOSED 2026-08-07 (assumption kept by user decision, now disclosed: `talentHitAssumed` + a banner clause; also pinned 33's owed ~119.31 end to end). `capStateFrom` folds the *pinned preset's* talent hit (3/3 Precision, ~47 rating) onto every ret character's gear, because `compose` never writes `talentsString` from the logged character — `talentsStringFromRequest` (`rank.ts:872`) says so in its own docstring. Filed 2026-08-07 from the `fix/carry-forward-backlog` review (adversarial A2). Blocking-class: a plausible number with no error. Sharpened by ticket 33 removing the banner's "talents not counted" hedge in the same branch — correctly per its own Done-when — so nothing user-facing flags the assumption any more. Also makes the cap error two-sided, undercutting the new docstring's "Heroic Presence is the only remaining uncounted source". WCL does carry real points (`CombatantInfo.talents[].id`, R18) but threading them crosses the compose stage
- 61 — CLOSED 2026-08-07 (guard wired into `rankUpgrades`; refuses on a named `detected` *or* `unsupported-spec`, since a prot paladin classifies as the latter; `LoggedGear.className` threaded from `actors[].subType`). `matchesRequestedSpec` is exported and unit-tested but has zero production callers, so ticket 40's named defect is still live: a ret request against a protection night sims against ret's preset and returns a confidently wrong ranking with no error. Filed 2026-08-07 from the `fix/carry-forward-backlog` review (adversarial A1). Not a dishonest close — 40 is correctly still open and names all four unmet criteria — filed separately because `spec.ts` now reads as a tested guard that guards nothing. Blocked by `LoggedGear` carrying no class name
- 62 — still open; sharpened 2026-08-07 with the measurement showing the `detected:<other spec>` branch is unreachable until a class ships two supported specs, so the fix is a rename plus `it.todo`, not a synthetic test. Two tests added on the carry-forward branch pass by construction: `spec.test.ts:100`'s name promises a mismatch against "a different supported spec" but the body asserts an ambiguous split and admits it (the `detected: <other spec>` branch is exercised by no test), and `view.test.ts:361` asserts two fields agree after ticket 36 made one a verbatim copy of the other. Filed 2026-08-07 from the `fix/carry-forward-backlog` review (adversarial A4, A5). The genuine version of the second already exists at `rank.test.ts:1899`
- 63 — `buildOfflineRecordings` (`report-events-offline.ts:99`) takes seven positional params, four of which (`route`, `confidence`, `notFoundMessage`, `killedAt`) are one "which route is this" decision travelling together; `notFoundMessage` is a callback for two constant strings. Filed 2026-08-07 from the `fix/carry-forward-backlog` review (standards S4). Judgement call, no correctness risk — ticket 38's dedup was right, this is the shape of the extracted signature. Filed fresh because ticket 24, the earlier standards-smells ticket, is closed and fully done
- 64 — a `spec-mismatch` throw (ticket 61's guard) is raised before `deps.store.job.create`, so unlike every other `RankError` it leaves no job row for the Phase 2 job API. Filed 2026-08-07 from the `fix/ticket-60-61-spec-and-caps` review (adversarial A4). Not urgent: the CLI is the only caller today and reports it correctly; it bites when Phase 3's web shell polls job status instead of an exit code. The guard's placement is deliberate (no row for work that never runs), so moving it is a sequencing decision, not a typo fix
- 65 — P3's raid zones are already covered (Black Temple and Hyjal Summit are both in `ret-p3.report.json`'s `phaseZones`, 364 entries), but the whole P3 universe holds **two** `rep` sources and both are P2 factions (Lower City, The Consortium). Ashtongue Deathsworn and Scale of the Sands appear nowhere as a source at any phase — the `ashtongue` grep hit in `db.json`/`items/index.json` is the item name "Staff of the Ashtongue Deathsworn" (31417), not a faction. Filed 2026-08-07 from the user's "add in-game phase 3 items" request. Blocked by 58: `rep` rows can only arrive through the three guide-prose regexes at `assemble_universe.py:263-289`, which is exactly the single-witness channel 58 wants replaced with a vendored reputation module. Carryover is not implicated — verified that both existing rep items reach P4 and P5. **Sized 2026-08-07** (`.scratch/carry-forward/notes/65-faction-overlap.py`): overlap is **zero** — 25 gear items to add (Ashtongue 9 talisman trinkets 32485-32493, Scale 16 Band of Eternity rings 29294-29309), none present in ret-p3 by any route, so the missing-rep-row count is the missing-item count. Recipes split: Scale's 30 are gem Designs whose outputs are already in `vendor/wowsims/db.json` (no work); Ashtongue's 17 are gear patterns whose outputs all resolve in the items index and none of which is in the universe (a second ~17-item body). Nothing blocks: zero-stat proc trinkets are already accepted (ret-p3 carries 28785/30447/30621) and `junkFilter.applied` is `false`. Upstream `AtlasLootClassic_Factions/data-tbc.lua` has both tables at the pinned commit, and `ItemSource` already has the `rep` variant — so step 1 is one line in `sync_atlasloot.py` TRACKED. **Corrected while planning**: only the Scale half needs 58. `vendor/wowsims/db.json` already carries `{"rep":{"repFactionId":1012,...}}` on all nine Ashtongue talismans and `assemble_universe.py:585` throws it away — that guard's comment claims it affects one item, it affects **111 across 10 factions**. Ashtongue needs a faction-id lookup table, not vendoring; Scale's rings really are `sources: null` and need the module. Plan at `.scratch/carry-forward/65-plan.md` sequences the cheap unblocked half first so ret's Talisman of Valor and feral's Talisman of Equilibrium land without waiting. Both specs have identical gaps — this is pipeline-wide, not spec-specific
- 70 — `parse_wowhead_source`'s quest-zone branch (`assemble_universe.py:1002`) scopes its two guards to the whole source text instead of the parenthetical `qm` matched, so a row carrying a real quest zone *and* any rep mention silently loses the zone (`'Quest: Foo (Nagrand), also sold by Vendor: Bar (Kurenai Honored)'` → Kurenai only). Second half: a parenthetical holding a bare standing with no faction falls through `STANDING_PAREN_RE` and fabricates a zone (`'Quest: A Reward (Honored)'` → `{kind:"raid", zone:"Honored"}`). Filed 2026-08-08 from the `feat/phase-3-vendor-and-craft-coverage` review (adversarial A1 + standards S3, found independently by both axes). Both reproduced live; both **latent** — measured zero at-risk rows across all 215 distinct `wowheadSourceText` values under `data/`. Blocking-class shape if reached: `zones_hit` misses `phase_zones`, the item drops out of a universe, no error. This is ticket 59's defect class, narrowed by the step-3 fix but not closed — 59 handled faction-plus-standing, not the bare standing, and became reachable last time precisely when new data arrived
- 71 — `parse_atlasloot.py` writes `args.out` at :519 and again at :545 after the faction merge, the first superseded, with `wrote {len(sources)} item keys` printed between them reporting a pre-merge count that never matches the bytes on disk. Filed 2026-08-08 from the `feat/phase-3-vendor-and-craft-coverage` review (standards S1). Harmless — the regen gate compares final bytes and the parse is deterministic (verified by running it twice into scratch dirs and `cmp`) — but two writes to one path plus a disagreeing count is exactly the ambiguity that makes a regeneration claim hard to audit, which ticket 68's rubric treats as first-class
- 2026-08-09 — Pre-merge review of `phase-2/trust` (4 axes, Opus, fresh context; `codex` not on `PATH`). Fixed on the branch: feral offline recordings never populated `className` (harmless in practice — druid talents can't distinguish cat/bear by design, so `classifyFeralForm`'s confidence score was already the real feral protection — but the field is part of the documented `LoggedGear` contract and the sibling fixture builder does populate it), and the CLI's offline-fixture matching ignored `--spec` so `--spec feral --character slamaltman` silently loaded ret's recording and failed with a misleading error. Also reworded `DEFAULT_SEEDS`'s comment (evidence file ran 5000 iterations, shipped default is 3000 — only the seed values transfer). Filed 74 (verification-log's ret-universe diff claim doesn't reproduce — real deletions where "zero deletions" was claimed; the most consequential finding), 75 (`--boss` has no validation, unlike `--raid`), 76 (HTML report missing hit-cap banner and fight-provenance warning that the CLI already has), 77 (unrounded `gapAfter` in the HTML report), 78 (`SqliteStore`/`CachingGearSource` exported and tested but unreachable from any production path), 79 (barrel-export inconsistency for offline fixture builders), 80 (four unused `RankErrorKind` variants; a bare `Error` escapes `RankError` handling), 81 (druid idols flagged `enchantable: true`, no current impact), 82 (verification-log's feral "no structural change" claim cites a now-dead command; claim re-verified true, citation stale). `pnpm land --check-only` green.
- 83 — `enchants.ts:35-38`'s `RangedWeaponType` constants are shifted one up against `data/proto/common.proto:359-369`: Bow declared 2 (really 1), Crossbow 3 (really 2), Gun 4 (really 3); `WAND = 5` is right only by coincidence. Measured at runtime with Adamantite Scope (2722): all 66 bows are **denied** scopes they take in TBC, and all 42 thrown weapons are **granted** scopes they never take. Filed 2026-08-09 from the `fix/75-82-review-tickets` review (domain D2). **Latent, not live** — counted across all six committed universes, the only `rangedWeaponType` values that ship are 6 (idols, feral) and 7 (librams, ret), so no current ranking is mis-scored; it bites when a hunter spec or any bow-admitting pool lands. Survived because the constants are a hand-copied second source of an enum the repo already vendors as a proto, and because ticket 81's new relic sweep passes either way — relics (6/7/8) sit outside both the wrong and the right shootable sets, so it pins the relic outcome and says nothing about the shootable branch
- 2026-08-09 — Pre-merge review of `fix/75-82-review-tickets` (4 axes, Opus, fresh context; `codex` not on `PATH`; diff scoped to `1b6dfb1...HEAD` because the branch stacks on the unmerged `phase-2/trust`, whose 154 commits already had their own review). Closed 74, 75, 76, 77, 79, 81, 82; left 78 and 80 open on their Phase 3 blockers. Fixed on the branch from review findings: the boss-match predicate was written three times (standards, Duplicated Code) — extracted `sourceMatchesBoss` in `pool.ts` and pointed `view.ts`'s `matchesBoss` and the pool test at it, so enumeration and filtering can no longer drift, which is the defect 75 exists to prevent one level down; a near-vacuous `expect(html).toContain("test")` (spec axis) tightened to the whole provenance sentence, since "test" also matches the footer's contentHash; and a **false durable claim** in the new `enchantable` docstring (domain D1) saying relics fail closed "because db.json ships no enchant whose type is 14" — `data/enchants/index.json` ships four type-14 scopes, and the real gate is an explicit shootable allowlist. Filed 83. `pnpm land --check-only` green.
- 84 — `pnpm verify` runs no proto check at all, so a stale or hand-edited `packages/core/src/proto/*.ts` passes every local gate; the regenerate-and-byte-compare lives only in `.github/workflows/verify.yml` (`pnpm proto:generate` then `git diff --exit-code -- packages/core/src/proto data/proto`), and the only proto `--check` script is `fetch:protos:check`, which checks the vendored `.proto` pin rather than the generated TypeScript. Filed 2026-08-09 from the `fix/75-82-review-tickets` review of 6fb9f1c (domain, spec and adversarial axes each reached it independently). Sharpened by ticket 83: `enchants.ts` now reads its enum *values* from that generated file at runtime, so staleness is a silent wrong-answer path rather than merely stale codegen. Ticket 83's test is a partial backstop — it pins real item ids from `data/items/index.json` against the enum, and a whole-enum shift that keeps test and production self-consistent still fails it (verified) — but only `RangedWeaponType` has that cover; EnchantType, WeaponType, HandType and ItemType have none
- 2026-08-09 — Pre-merge review of 6fb9f1c on `fix/75-82-review-tickets` (4 axes, Opus, fresh context; `codex` not on `PATH`; diff scoped to `5c14b9d...HEAD`, the one commit added after the branch's earlier review). Adversarial found nothing, having empirically rejected the test-theatre hypothesis: shifting the whole generated enum so test and production stay self-consistent still fails the test, because the item-id assertions are anchored to `data/items/index.json`. Domain confirmed the off-by-one and all eight item->type fixtures against vendored data, and confirmed only four type-14 enchants ship, all scopes. Fixed on the branch from review findings: the new docstring claimed `pnpm proto:generate` was what stopped drift when the actual gate is the CI byte-compare (domain + spec, independently) — reworded to name the CI step and the `pnpm verify` gap; the nine module-level constants that merely renamed imported enum members were deleted in favour of inline `RangedWeaponType.RangedWeaponTypeBow` at the call sites, matching `gems.ts`/`meta.ts`/`meta-repair.ts` (standards, Middle Man, argued from repo precedent rather than taste — the `ENCHANT_TYPE_*` exports turned out to be vestigial, absent from the barrel with no consumers); `RELIC_RANGED_TYPES` in the test still hand-wrote `[6, 7, 8]` six lines above a test importing those same values from the proto (domain, the very pattern 83 set out to remove); and ticket 83's `Resolution:` block asserted "failed on the bow before the fix" and "`pnpm verify` green" with no re-runnable command (standards, durable claims) — now carries the repro command and says which environment was observed. Filed 84. `pnpm land --check-only` green.
- 85 — `check_merge_ready.py:60` parses only the FIRST `## Disposition` section (`re.search` plus a `(?=^## |\Z)` lookahead), so a review file with a second table has every row in it skipped — `defer` rows included — while the gate still prints `merge-ready: ok`. Filed 2026-08-09, found by running `pnpm land --check-only` on `fix/75-82-review-tickets` after appending a second-pass review: it reported `disposition rows: 14` and passed, with nine rows and one open-ticket `defer` unchecked. Silent and misleading in the wrong direction — the more thorough the second review pass, the more rows go unverified, and this is the last automated gate before a merge to `dev`. Worked around on that branch by folding the rows into one table with `2-` prefixed ids (a second table naturally reuses A1/D1), so `docs/reviews/` no longer reproduces it; fix is `re.finditer` over all sections plus a duplicate-id check
- 2026-08-09 — Pre-merge review of `feat/set-bonus-value` (4 axes, Opus, fresh context; `codex` not on `PATH`). Two blockers, both fixed on the branch: the `withSetPotential` shortlist filtered on the raw cutoff and hid exactly the below-cutoff-alone tier pieces the toggle exists to surface (now re-derived from the effective value against the same absolute cutoff), and the V0b Thunderheart gate evidence was confounded by breaking the baseline's active Malorne 2pc (re-run confound-free as V0c: Malorne 2pc→4pc, synergy 20.89 vs 3σ 18.03, pass). Also fixed: `nextThreshold` off `piecesWornBefore`, under-computed synergy `se`, set-id-in-item-id failure rows, deltaDps-ordered `packageItemIds`, Σ-singles test theatre, packages now record other-set bonuses they break (`SetBonusValue.breaks`), acceptance §8.2/§8.4 evidence persisted. Filed 86 (`piecesAfterSwap` owned-proxy), 87 (set-value module shape cleanups). `pnpm land --check-only` green.
- 100 — the Set potential panel is the only surface a set-completion bonus can reach once ticket 91 shipped package-as-card (a 4pc at 0 pieces worn is credited to no row by construction), but the panel is gated on `withSetPotential`, which `spec.md:95` defines as **default off** (`rank-report.ts:382` and `:209`). So the figure that reaches no row also reaches no default reader, and ticket 90's "still disclosed in the Set potential panel" inherits the gap. Sharpened by contrast: ticket 98's plausibility panel renders unconditionally, so a warning naming what a package breaks can render while the package it names does not. Filed 2026-08-10 from the `feat/set-bonus-value` pre-merge review (spec axis). Not simply "flip the default" — default-off is a deliberate spec decision that also gates the per-row column and the sort key; the likely fix is decoupling the disclosure panel (which moves no number) from the sort/column toggle, but that needs a decision rather than a reviewer's guess. Resolve before ticket 96, whose contradiction this panel is supposed to explain
- 101 — `docs/adr/` records nothing about set bonuses (`grep -ril "set bonus\|setBonus" docs/adr/ CONTEXT.md` is empty), though the threshold-selection rule has now been decided once (spec.md §2.3) and mis-remembered once (the `brokenSetBonuses` docstring's "charged twice", fixed in `2e0b499`). Ticket 93 asked for the ADR once 90/91 landed; both landed and it was not written. Should record the threshold-selection rule including the case spec §2.3 omitted (at 0 worn with an implemented 2pc, a single swap structurally cannot reach the 4pc), why per-row credit was rejected for package-as-card, that the strategy is suppress-and-disclose rather than correct-and-disclose (no numeric correction anywhere, because `B` is gear-dependent), and the `(k−1)·B` closed form with k=0 and k=1 both inflating by exactly zero. Filed 2026-08-10 from the `feat/set-bonus-value` pre-merge review (spec axis)
- 102 — `CURATED_SET_PHASE` in `rank-report-rules.ts:219-223` is a hand-copied mirror of `scripts/assemble_universe.py:354` and has drifted: the Python reads `{"preraid": 1, "p1": 1, "p2": 2, "p3": 3}`, the TypeScript stops at `p2`, and the branch that pinned P3 curated sets (`02f2f85`) did not update it. Consequence is a silent suppression in the safe-looking direction: a future P4 rank falls back to a `p3` label (`assemble_universe.py:396-403`), `curatedSetPhase("p3")` returns `null`, `bisStale` computes `false`, and the "No curated set is pinned for P4" warning at `rank-report.ts:442-444` does not render — showing a P3 BiS list under a P4 heading with no staleness note, the exact overclaim that warning exists to prevent. Latent today (feral maxes at p3 vendored); bites when p4 is pinned. Note the wrong behaviour is test-locked at `rank-report.test.ts:1363` (`curatedSetPhase("p3") → null`), so the drift was asserted rather than caught. Filed 2026-08-10 from the `feat/set-bonus-value` pre-merge review (domain axis). Real fix is generating the mirror rather than hand-copying it — the repo already gates generated literal types via `scripts/generate_json_literal_types.py`
- 2026-08-12 — Pre-merge review of `feat/set-bonus-value`, round 3 (4 axes, Opus, fresh context; `codex` not on `PATH`). Scope was the 68 commits after round 2's tip `cfc77c9`: tickets 111/112, the 115-123 fix round, the ret catch-up, the ADR-0023/0024 and spec amendments. Tests under Node 22.16.0 — `pnpm verify` exits 0, 38 files, 696 passing, 2 todo. One blocking finding, same shape as round 2's: ticket 119 correctly stopped printing a fabricated `0.00` for an unmeasurable 2-piece threshold, and that fake zero was the only visible hint that the 4-piece figure above it depended on it. The 4-piece number now prints bare with a standard error in `slamaltman-p3.json`/`.html` while missing its 2-piece term — confirmed against the artifact's own numbers (`-26.77179572189698 - (-16.847102266916636) - 0` reproduces the reported `-9.924693454980343` exactly, and `twoPieceBonus` is absent from the entry). Domain found no contradictions of established facts at all: gem-cap solvability, the Jewelcrafting drop, byte-identical palette regeneration and ticket 122's Go claim all verified against the pin. Standards found four stale descriptions left behind by ticket 117 (`gems.ts:47` still says meta repair does not cap rarity; two collided comment blocks in `rank-report.ts`; a test name asserting removed behaviour) plus a CRLF flip of `package.json`'s committed bytes. Spec found ADR-0023 decision 5 contradicting ADR-0024 and the shipped code. Filed 127-134. Nothing fixed in this round — findings go to a separate judge
- 127 — a set bonus computed without its own lower-threshold term prints as an ordinary measurement with a standard error and no qualifier anywhere. Root arithmetic is ticket 119 anomaly A (`rank.ts:1067-1088`'s one-piece early return happens before `:1162` records `twoPieceBonus`, so `set-value.ts:353` subtracts a missing term as zero); what 127 adds is that no surface discloses it, because `formatBreaksPrefix` and the panel qualifier key off cross-set `breaks` and a self-set confound produces none. Live in `slamaltman-p3.html:645` ("set bonus -9.92 DPS") four lines under `:641`'s "can't be measured from this starting gear" for the same set. A disclosure regression, not an arithmetic one — the fabricated `0.00` ticket 119 removed was the only visible trace of the coupling. Blocked by 119, whose step 2 owns the suppress-vs-qualify decision. Filed 2026-08-12 from the round-3 pre-merge review (adversarial axis)
- 128 — `rank-report-css.ts:222`'s `body.package .chip.package-only` is more specific than `.source-hidden` (`:444`) and `body.bis-only .chip:not(.is-bis)` (`:452`), so in package mode a revealed chip ignores the source and BiS filters while the report's JavaScript correctly excludes it — the chip renders with a blanked position number and is absent from `list-count` and `exportChips`, making the Export caption's "as currently filtered and sorted" false against what the reader sees. Reachable today: chips 30989 and 30997 are non-BiS and single-sourced. Filed 2026-08-12 from the round-3 pre-merge review (adversarial axis)
- 129 — `c2d3897` flipped `package.json`'s committed bytes from LF to CRLF (parent blob 0 CRLF, commit blob 67; `core.autocrlf=false`; `.gitattributes` covers only the two proto trees), turning a one-line script addition into a 67/66 whole-file rewrite and making the CRLF permanent until someone on a POSIX box flips it back. The AGENTS.md loop-step-2 case where an unrelated whole-file change rides along and hides the real one. Filed 2026-08-12 from the round-3 pre-merge review (standards axis)
- 130 — ticket 117 made both `repairMeta` call sites draw from the rare-capped palette and left four descriptions asserting the opposite: `gems.ts:47` ("the auto-fill path caps rarity; meta repair does not"), two collided comment blocks at `rank-report.ts:340-346` (the first describes `curatedPointer` twelve lines below; the second says the line renders only for a positive package, which ADR-0024 explicitly contradicts), and `candidate-gems.test.ts:143`'s name promising behaviour that was removed. Also records that no gem-selection path reads `ctx.palette` any more — its only consumer is `rank.ts:527`'s `gemPaletteIds`, so the content hash keys off the uncapped palette while selection uses the capped one. Filed 2026-08-12 from the round-3 pre-merge review (standards axis)
- 131 — ADR-0023's header reaffirms "Decisions 1, 3, 4 and 5 stand as written" while decision 5 says a row "never restate its figure… It carries no number", and ADR-0024's 2026-08-11 amendment item 5 plus the shipped `formatCuratedPackagePointer` do exactly the opposite. Defensible in substance — the restated figure is the directly measured `packageDeltaDps`, not the break-confounded `bonusDps` decision 5 was protecting — but undocumented, so the two ADRs give opposite instructions and the one claiming to stand is not the one the code follows. One sentence narrowing decision 5 to `bonusDps` closes it. Filed 2026-08-12 from the round-3 pre-merge review (spec axis)
- 132 — tickets 111 and 112 are `Status: closed` with `Closed:` commit lists and all 14 acceptance boxes between them unticked, so the files no longer record which criteria were verified. Matters because those two tickets are what a future reader consults to learn what the rare-gem cap was required to do, and ticket 130 exists because the same round left four descriptions of that cap stale. Filed 2026-08-12 from the round-3 pre-merge review (spec axis)
- 133 — the negative side of the set-bonus plausibility band reuses the 7.5% width that `plausibility.ts:28-41` calibrates from two *positive* anchors (Thunderheart 4pc at 9.0% must fire, Malorne 2pc at 5.9% must not), while ticket 120's closing note claims it was "calibrated the same way ticket 98's positive band was" — it was not, and no negative bonus has been measured on either side. Unknown false-negative rate in the direction the gate was added for: on a 2150 baseline a distortion between roughly -7.5% and -160 DPS passes silently and is then presented as a measured bonus. The acceptance test pins only the extremes, which the positive width would already have separated. Low ranking risk (the gate warns, it does not score); wants doing alongside 127, whose confound is the mechanism 120's wording blames. Filed 2026-08-12 from the round-3 pre-merge review (domain axis)
- 134 — ticket 125 asked that usage output name `--spec`, its accepted values, *and* its default; `cli.ts:79` prints `[--spec ret|feral]` without the default (`ret`, `cli.ts:114`), while 125 is closed with a note claiming completion and both acceptance boxes unticked. One string. Filed 2026-08-12 from the round-3 pre-merge review (spec axis)
- 135 — set-bonus package repair failures are recorded `unmeasured: "sim-failed"` when no sim ran; `UnmeasuredReason` needs a `"repair-failed"` value and the `buildSetBonuses` push site updated. Filed 2026-08-12 from the round-4 pre-merge review (spec axis)
- 136 — batched issue-1 cleanup follow-ups: unify the two socket-bonus predicates, extract `repairAndMinimize`, fold the three skip arrays, collapse the duplicated five-seed scripts, and add a fixture that actually exercises the changed socket-bonus branches (the round-4 blast-radius check was a null result). Filed 2026-08-12 from the round-4 pre-merge review (standards + domain axes)
- 137 — check_rename_contradictions.py prints UTF-8 to a cp1252 console, and its one-line regex misses a flattening across a wrap (docs/reviews/docs-terminology-cleanup-plan.md)
- 138 — "stage" now means delivery step, pipeline step and content tier; CONTEXT.md documents two and resolves them with the capitalisation rule it elsewhere rejects (docs/reviews/docs-terminology-cleanup-plan.md)
- 158 — `data/universes/*.json` record no EP-weights provenance, so a reweight is invisible in the artifact. `assemble_universe.py:1303` resolves weights via `ep_weights_path_for` (highest key <= max_phase), so after `feat/ret-p3-data` ret p4/p5 score against `p3.ep-weights.json` while `generatedBy` stays the bare string `"scripts/assemble_universe.py"` and the report records nothing. Measured on the branch: 423 of 534 p5 `curationHint` values changed with zero membership change, and `git diff 5be6a81...859eab5 --stat -- data/universes/` shows p3/p4/p5 `.report.json` absent from the diff entirely — a reader diffing reports sees nothing. `curationHint` is the EP score, the slot sort key and the junk-filter percentile floor. The substitution itself is the *better* approximation (domain axis); the missing provenance is the defect. Also folds in the dead `candidates` branch in `ep_weights_path_for`, the stale `ep_weights_for_phase()` name at `:171`, and the un-checksummed hand-transcribed `p3.ep-weights.json` (no lock entry, unlike `ret_p3.gear.json`). Filed 2026-08-14 from the `feat/ret-p3-data` pre-merge review (adversarial + domain independently, plus spec's provenance-note gap)
- 159 — `cli.ts:284-288` picks EP weights by spec only and ignores `args.maxPhase`, so a `--max-phase 3` ret rank prefilters and gem-fills with `p2.ep-weights.json` while `assemble_universe.py` now scores the p3+ universes with p3's. Two disagreeing sources for one fact. Pre-existing (`git show 5be6a81:packages/core/src/cli.ts` is byte-identical and the branch changed no file under `packages/core/src`), but `feat/ret-p3-data` is what makes them disagree. The CLI also reads only `.weights`, never `pseudoWeights`, so the main-hand-DPS term reaches `curationHint` but not the CLI path — confirm intent before changing. Fix wants one shared mapping, not a hand-copied TS table (see ticket 102 for that failure mode). Filed 2026-08-14 from the `feat/ret-p3-data` pre-merge review (reviewer's own check)
- 160 — `PER_FILE_PIN` (`sync_wowsims.py:122-141`, plus the per-entry `"commit"` written in `do_update` and read in `do_restore`) has no case in `check_sync_wowsims.py`, which is the `pnpm verify`-gated check — `grep -n "PER_FILE_PIN" scripts/check_sync_wowsims.py` is empty. Nothing fails if a future `--update` drops the override and drags `ret_p3.gear.json` back from `5c7491899` to the main pin, the exact hazard the new comment warns about. Currently correct and hash-guarded by `verify_blob` (adversarial axis confirmed the round-trip), so this is missing regression cover rather than a live bug; the slice handoff itself records a second override entry as **untested**. Filed 2026-08-14 from the `feat/ret-p3-data` pre-merge review (standards axis)
- 161 — two stale documents, no behaviour wrong. PLAN.md §16 item 3 still asserts `p2.ep-weights.json` "is missing its largest term" when it has carried `pseudoWeights: {"0": 5.34}` since `2fdad02` (2026-08-02), and `docs/plans/wowsims-tab/plan.md:322` inherited the claim as "fix while here". Separately `slice-6/HANDOFF.md:664-665` says `sme-rank-review` "has not been run" while the verdict is committed in the same branch at `859eab5`. The related scope gap — plan §9.6 asks for a review of a *ranking*, the filed verdict reviewed a candidate list and answered "Not yet" — is deliberately NOT ticketed here; it is a merge-ask decision raised in the review's Summary. Filed 2026-08-14 from the `feat/ret-p3-data` pre-merge review (spec axis)
- 2026-08-14 — Pre-merge review of `feat/ret-p3-data` @ `859eab5` (3 axes, Opus effort medium, fresh context, one parallel batch; `codex` not on `PATH`). Range `5be6a81..859eab5`, slice 6/6b of the wowsims-tab detour. Domain found **no contradictions** of stage0-findings/verification-log — the diff reads no WCL field, no slot mapping, no race or spec inference, and `currentPhase` still comes only from `constants_other.ts`, which `PER_FILE_PIN` deliberately does not override. Two reviewers independently regenerated all four ret universes byte-for-byte, matching the orchestrator's slice-6b table. Verified by the review author rather than taken on the axes' word: vendored `ret_p3.gear.json` hashes to its lock row (647 bytes, `673db01…`), the 15 p3 tags are exactly upstream's 16 populated slots minus `27484` with zero spurious, main pin unmoved with exactly one override, phase guard correct both directions. Top finding is a disclosure defect in the same family as 127/102 — p4/p5 reweighted with nothing recording it. Nothing fixed on the branch; filed 158, 159, 160, 161. Ticket 157 (SME pool membership) and 154 (feral-p2 staleness) excluded as pre-existing
- 165 — E-W3 (`packages/core/test/wowsims-fork-parity.test.ts`) cannot fail on any module that only *composes* the sim request. `buildRecordingsAndRun` derives recording keys from equipment the engine under test computed, so a gem-fill/meta-repair/enchant mutation moves the key and the lookup together and still hits; only arithmetic downstream of the observation (`se.ts`, `cutoff.ts`, `set-value.ts`) can fail. The test header's "a behaviour-changing edit to either copy fails this test" is untrue for roughly half the ported modules. Partially mitigated at `5cb013c` (standing-assumption parity, verified by mutation). Filed 2026-08-14 from the `feat/sweep-tab-tickets` pre-merge review (adversarial axis)
- 166 — `vendor/` is gitignored but not excluded in `vitest.config.ts`, so vitest collects the fork's `node:test` file, registers zero tests, and reports "1 passed". A deliberately broken assertion still passes `pnpm verify`. Ticket 162's only test is ungated in every state. Filed 2026-08-14 from the `feat/sweep-tab-tickets` pre-merge review (standards axis)
- 167 — `check_engine_port_drift.py` sha256s raw bytes against LF-computed hashes, so an ordinary Windows `git checkout` of a ported engine file fails the gate on line endings alone, indistinguishably from real drift. Hit twice during the sweep fan-in; normalizing back to LF restored the recorded hash byte-exact, which is the proof the content never changed. Filed 2026-08-14 by the orchestrator during fan-in
- 168 — latent cross-branch disclosure defect. The tab's drawer names `ret-p2.ep-weights.json`, correct on `feat/sweep-tab-tickets` (no p3 file, assembler hardcodes p2) but wrong once `feat/sweep-ret-tickets` merges (p3 weights score ret p3/p4/p5). Whichever sweep branch lands second must make `EP_WEIGHTS_SOURCE_BY_SPEC` resolve by spec *and phase* from the shared `ep-weights-by-phase.json`. Appears at merge time without either branch changing. Filed 2026-08-14 from the `feat/sweep-tab-tickets` pre-merge review (domain axis)
- 2026-08-14 — Pre-merge review of `feat/sweep-tab-tickets` @ `cab44cb` (3 axes, Opus effort medium, fresh context, one parallel batch; `codex` not on `PATH`). Range `5be6a81..cab44cb`, the ticket-sweep slice covering 155, 156, and 162 v1. Two adversarial blockers, both verified by mutation rather than argued: E-W3 could not fail on `disclosure.ts` or `rank.ts` (gutting every standing assumption still passed) — which matters because those two files' `PROVENANCE.md` hashes had just been re-blessed on that green run — and the fixture is self-keying, so request composition is structurally invisible. Fixed the first at `5cb013c`; ticketed the second as 165. Domain verified the parity fixture's set/socket facts correct against `data/items/index.json` and found the EP-weights disclosure misleading by omission (within-slot ordering is an EP artifact), fixed in fork commit `63494ce7b`. Note that ticket 162's implementation lives entirely in `vendor/tbc-new-fork`'s own history and is invisible to this repo's `git log`. Filed 165, 166, 167, 168
- 170 — `check_restore_fetches_override_not_main_pin` in `scripts/check_sync_wowsims.py` dies with an unhandled `FileNotFoundError` in its own harness when `do_restore`'s override read is broken, printing zero FAIL lines. Exit is 1, so the gate holds and `pnpm verify` fails — an earlier reading claiming exit 0 / "guard rails ok" does not reproduce. What is broken is the diagnosis: the traceback names a temp path, not the mechanism. Filed 2026-08-14 from the `feat/sweep-ret-tickets` pre-merge review (adversarial axis)
- 171 — three librams (28592, 30063, 32368) share `deltaDps` to 16 significant figures because their procs exist only as commented-out stubs in the pinned fork, and the ret-p3 report renders them with ordinary `delta down` loss styling and no disclosure. The relic slot's presentation regressed as its data improved: before ticket 157 it carried a dead-slot retraction, and now that 27484 is a pool member the guard correctly has nothing to fire on. Third case alongside 164 (unmeasured slots) and 169 (candidates never offered). Filed 2026-08-14 from the `feat/sweep-ret-tickets` pre-merge review (domain axis)
- 172 — no `pnpm verify` step regenerates `data/universes/**` and byte-compares against the committed artifact, so assembler changes can leave universes stale with every gate green. This class of drift has now recurred twice: ticket 154's original cause, and ticket 157's ungated force-include two commits after 154 fixed it. Filed 2026-08-14 by the orchestrator during the `feat/sweep-ret-tickets` review
- 173 — ticket 157 rescued six items by name; the general source-parse defect is untouched and has no detector. `map_db_source` still cannot resolve five-man zones outside `PHASE_HEROIC_DUNGEONS` and `WOWHEAD_HEROIC_ZONE_RE` still misses a " - Heroic" suffix, so the next such item is dropped silently. 27878 and 29247 are already in that state; 23206 and 19343 show the exclusion line is era judgment, not a parser rule. Suggested tripwire: pin `excludedNoSource`'s size in `pool-hardening.test.ts`. Filed 2026-08-14 from the `feat/sweep-ret-tickets` pre-merge review (adversarial + domain axes)
- 174 — all six ticket-157 force-included items ship `{"kind": "unknown"}` while their origin is recorded in `TICKET_157_FORCE_INCLUDE`'s own dict values, because the closed `ItemSource` vocabulary has no variant carrying prose. Surfaced when 28034 legitimately entered the feral universe and failed `pool-hardening.test.ts`'s "reserves kind:unknown for items no input mentions at all". Verified not caused by the ret gate — ungated, 28034 still ships `unknown`. Excepted by id at `57adad6`. Filed 2026-08-14 by the orchestrator during fan-in
- 2026-08-14 — Pre-merge review of `feat/sweep-ret-tickets` @ `952fdc6` (3 axes, Opus effort medium, fresh context, one parallel batch; `codex` not on `PATH`). Range `5be6a81..952fdc6`, the ticket-sweep slice covering 154, 157, 160, 161, 163, 164. One blocker: ticket 157's `TICKET_157_FORCE_INCLUDE` had no spec gate, so three ret trinkets leaked into the feral universes and put feral-p2/p3 out of step with their generator — two commits after ticket 154 fixed exactly that, with `pnpm verify` green throughout because nothing compares a committed universe against a regen. Fixed at `b2da640`; the missing gate is ticket 172. That fix surfaced ticket 174 (force-included items claim unknown origin they have). Ticket 154's "253 is correct, 256 traces to another branch" verdict was unevidenced and wrong — the reconciled answer is 254, corrected at `a448466`. Adversarial's reported `do_restore` false-pass did not reproduce (exit is 1, not 0) and was filed at corrected severity as 170. Domain's D3 — three unsimulated librams rendered as ordinary losses in the slot that blocked the SME gate — is the finding most worth the user's attention. Note this branch is a strict superset of `feat/ret-p3-data`. Filed 170, 171, 172, 173, 174
- 2026-08-18 — Pre-merge review round 3 of feat/candidate-pool @ 8cb5e68 (adversarial, domain, standards, spec; four fresh Opus subagents; codex not on PATH). Range 8867f5c..8cb5e68, tickets 156–225 incl. the stage-gated 223, 224, 225. Fixed A1, A2, A3, A4, D1, D2, P7 at dc49801..1d56e5f — the feral ratios 0.6457 and 0.9837 now reproduce from measure-racing-ratio.ts with the feral-p3 or feral selector, and ticket 228 was corrected against druid.ts and retargeted at assemble_universe.py. Filed 229 (measure-* copied prologue + SE constant), 230 (7.3 name collision), 231 (section 10 REPORT.md and ticket 219 closure — user decision). Merge-relevant open finding: 228 (druid-illegal axe + sword in the feral ranked list)
- 2026-08-19 — Pre-merge review of `feat/seed-overlap` @ `1c11472` (adversarial, domain, standards, spec; four fresh Opus subagents, effort medium; `codex` not on `PATH`). Range `3d64d1f..1c11472`, ticket 236 only — the branch sits on ~165 commits not in `dev`, so a `dev...HEAD` diff would have shipped reviewers the whole candidate-pool epic; that range is **not** gated by this review file. Ticket 236 itself (replication seeds shared RNG streams, so the paired-replicate SE was far too small) was confirmed against upstream source: `sim.go:248-251` seeds iteration `i` from `RandomSeed + i`, and `sim_concurrent.go:39-40` shows upstream spacing its own concurrent splits by exactly the iteration count. One blocking defect found and fixed: the new derived defaults were frozen against `DEFAULT_ITERATIONS` at module load, so `iterations: 5000` produced under-spaced seeds and threw `RankError` on a valid call — the fix re-created the drift the ticket set out to remove, and now has a regression test proven red against the bug. Also fixed: a half-migrated `BASELINE_BY_SEED` that flattened two tests' baselines, a `skipIf`-gated parity suite broken for anyone with the fork checked out, `replicateSeeds` missing from the barrel, and the retracted 0.099 conclusion still live in `PLAN.md:717`. Two acceptance boxes demoted from `[x]` to `[~]` after the spec axis showed they had been reworded by the same change that ticked them. Deferred: the paired-replicate SE evidence is annotated, not re-derived — carried on ticket 233. No new tickets filed
- 238 — `pnpm merge-to-dev --check-only` exits 1 on **every** branch: ticket 228's `Status:` line is prose ("blocked on a user decision — …") and `scripts/check_merge_ready.py:57` accepts only open/claimed/closed/resolved/wontfix, so the gate refuses a status it cannot read. Pre-existing since `6772904`/`57350d9`; surfaced by the `feat/seed-overlap` merge check on 2026-08-19. Not fixed in place because the choice — flatten 228 to `open`, or add `blocked` to the vocabulary and move `docs/agents/issue-tracker.md` with it — is a judgement about 228's own user-blocked state. Filed 2026-08-19
- 242 — the ret weapon-exclusion set excludes staff (8) only, but upstream `paladin.ts:26-33` omits dagger (2) and fist (3) from paladin's eligible types exactly as it omits staff, so `data/weapon-type-exclusions.json` publishes `"ret": [8]` where its cited source supports `[2, 3, 8]`. Latent: the ret profile's `allow_one_hand=False` gate runs before the weapon-type check, and every TBC dagger and fist is one-hand, so the committed ret universes carry no type 2 or 3 today. Becomes live if `allow_one_hand` ever flips for ret — the same shape as ticket 228, which was a blocker. Also means `weapon-type-exclusion.test.ts` certifies ret clean against an incomplete rule. Filed 2026-08-20 from the `feat/candidate-pool` round-4 pre-merge review (domain axis)
- 243 — `assertUsableSeeds(seeds, iterations?)` gates ticket 236's whole spacing check behind `if (iterations === undefined) return;` (`se.ts:63`), so the guard against a false-precision SE — PLAN.md's stated worst case — defaults to off, on a function exported from the public barrel. Behaviour today is correct: the one production caller (`rank.ts:684`) passes it. The optionality is documented ("callers that cannot know the iteration count keep the old contract", `se.test.ts:118`) but no such caller exists. Three options recorded on the ticket rather than one picked, because the comment implies an intended caller whose identity should be confirmed first. Filed 2026-08-20 from the `feat/candidate-pool` round-4 pre-merge review (adversarial axis)
- 2026-08-20 — Pre-merge review round 4 of `feat/candidate-pool` @ `88c1c1e` (adversarial, domain, standards+spec; three fresh Opus subagents, effort medium, one parallel batch; `codex` not on `PATH`). Range `8cb5e68..88c1c1e`, 57 commits / ~766 KB, chained from round 3's recorded through-sha. The diff was split into three slices (src+test, scripts+data+docs, infra) because 766 KB does not fit one reviewer. **No blockers on any axis.** The racing removal — the window's highest-risk item — was verified consistent: no residual `screened`/`ruledOut` in `src` beyond the deliberately-frozen cache-key nulls, and `candidate-pool.md` §6 is explicitly superseded by ADR-0026 and records where the recall obligation moved. Adversarial independently re-derived the frozen `contentHash` constant from a `8cb5e68` worktree and got a byte-identical hash, so no stored ranking silently re-sims. Domain confirmed the universe shrink (p2 246→228, p3 398→365) removes only weapon rows of the excluded types, zero items added — and caught the polearm recall error being a real druid exclusion, per `druid.ts`. Fixed in place: ticket 230's stale acceptance box (its blocker cited `racing.test.ts`, deleted at `28b00f9`) and ticket 236's prose `Status:` line (moved the qualifier to a `Note:` line; it had survived the gate only because `STATUS_RE` captures `(\S+)`). Filed 242, 243. Open decision for the owner: ticket 234 (whether role-inappropriate mana-driven items should be pooled at all), which blocks 227
